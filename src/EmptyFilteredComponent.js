/**
 * This will not be rendered/called if there is no filter in use,
 * unless the filter calls the component.
 *
 * @param {Object} props
 */
export const EmptyFilteredComponent = ( props ) => {
	return <></>;
};
