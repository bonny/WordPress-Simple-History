// Stub component — replaced by Task 3.
import { __ } from '@wordpress/i18n';

export default function CalendarView() {
	return (
		<div className="sh-calendar-stub">
			{ __( 'Calendar view coming soon.', 'simple-history' ) }
		</div>
	);
}
