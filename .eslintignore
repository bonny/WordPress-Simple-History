build
node_modules
vendor

# Third-party plugins used as test fixtures — not our code to lint or fix.
tests/plugins

# Generated Playwright HTML report, including its bundled vendor JS.
playwright-report

# Minified vendor bundles. Running --fix on these pretty-prints them into
# tens of thousands of lines of unreviewable diff.
*.min.js
